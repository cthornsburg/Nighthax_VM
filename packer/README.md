# Packer build (VMware)

This folder will contain the Packer template(s) to build the NighHax VM appliance.

Target:
- Ubuntu 24.04 LTS + XFCE
- VMware-first (Workstation Player/Pro), exportable to OVA

Status: template scaffolding in progress.
